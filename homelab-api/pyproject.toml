[project]
name = "homelab-api"
version = "0.1.0"
description = "Self-documenting API for homelab setup"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
dev-dependencies = []
